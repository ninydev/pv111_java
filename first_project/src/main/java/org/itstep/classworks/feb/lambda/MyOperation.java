package org.itstep.classworks.feb.lambda;

/**
 * Для создания лямбда выражения (оператора) необходимо описать 1 метод в интерфейсе
 */
public interface MyOperation {
    double operation (Double a, Double b);
}
